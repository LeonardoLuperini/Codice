#include <stdlib.h>
#include <math.h>

/* avg stand for AVeraGe
 * Take the arry and the length. 
 * Return the average of the numbers in the array.
 */

double avg(double numbers[], size_t len);
double std(double numbers[], size_t len);
